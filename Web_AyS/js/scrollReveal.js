window.sr = ScrollReveal({reset:true});

var servicio = {
  distance: '100%',
  origin:'bottom',
  duration: 1000
}

sr.reveal('.containerServicio', servicio);
sr.reveal('.row',{
  duration: 2000,
});
